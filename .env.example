# Example environment variables for StreamFlix Backend

# MongoDB connection string (use SRV connection for Atlas if needed)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/streamflix?retryWrites=true&w=majority

# JWT secret for signing tokens
JWT_SECRET=replace_with_strong_secret

# Optional SMTP settings for password reset
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your-smtp-user
EMAIL_PASS=your-smtp-password

# Optional Redis URL for production token blacklist
REDIS_URL=redis://:password@redis.example.com:6379

# App port (optional)
PORT=3000

# Allowed CORS origin for the Vercel frontend (replace with your frontend URL)
FRONTEND_ORIGIN=https://your-frontend.vercel.app
